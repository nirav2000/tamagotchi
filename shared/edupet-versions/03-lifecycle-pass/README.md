# EduPet 03 - Lifecycle pass

Source branch: `assistant-lifecycle`

## Intended source files

This version points at the lifecycle extension work added on top of the base EduPet library.

Key files:

- `shared/edupet/LIFECYCLE.md`
- `shared/edupet/edupet-lifecycle.js`
- `shared/edupet/demo/lifecycle.html`

## Status

This is the lifecycle and branching-character pass.

## Notes

This variant adds:

- egg-to-death lifecycle planning
- branching character forms
- hidden lifecycle scores
- health states
- death, memorial and rebirth behaviour

It is designed as an extension layer rather than a full replacement of the current base files.
